
console.log('Processo de renderização')

// Função para abrir a janela de Cadastro de Clientes
function cliente(){
   // console.log('Teste do botão cliente')
   // Uso da API (autorizada no preload.js)
   api.clientWindow()
}

// Função para abrir a janela de Ordem de Serviço (OS)
function os(){
    // console.log('Teste do botão OS')
    // Uso da API (autorizada no preload.js)
    api.osWindow()
}

// Função para abrir a janela de Configurações
function configuracoes(){
    // console.log('Teste do botão Configurações')
    // Uso da API (autorizada no preload.js)
    api.configuracoesWindow()
}

// Função para abrir a janela de Suporte
function suporte(){
    // console.log('Teste do botão Suporte')
    // Uso da API (autorizada no preload.js)
    api.suportetWindow()
}

// Função para abrir a janela de Relatórios
function relatorios(){
    // console.log('Teste do botão Relatórios')
    // Uso da API (autorizada no preload.js)
    api.relatoriosWindow()
}

// Função para abrir a janela de Ajuda
function ajuda(){
    // console.log('Teste do botão Ajuda')
    // Uso da API (autorizada no preload.js)
    api.ajudaWindow()
}

// Função para abrir a janela de Ajuda
function listacliente(){
    // console.log('Teste do botão Lista de Cliente')
    // Uso da API (autorizada no preload.js)
    api.listaclienteWindow()
}

